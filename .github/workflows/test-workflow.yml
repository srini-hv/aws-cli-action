name: AWS CLI test workflow

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:

  checkout-tests:
    runs-on: ubuntu-latest

    steps:
      - name: AWS CLI Action
        uses: ./
        with:
          command: --version